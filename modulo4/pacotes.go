//go:build ignore

package main

import (
	"fmt"
	Erick "strings"
)
func main() {
		fmt.Println("Hello, world!")

		// sem alterar o nome do pacote,
		// usaríamos chamando strings.Func()
		// strings.Split()
		fmt.Println(Erick.Split("Erick", ""))
}